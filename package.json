{
  "name": "f1shproxy",
  "version": "1.0.0",
  "description": "simple proxy thingy for vercel",
  "main": "src/index.js",
  "scripts": {
    "start": "vercel dev",
    "push": "git add . && git commit -m \"too lazy to write a commit message\" && git push"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "node-fetch": "^2.6.1"
  }
}